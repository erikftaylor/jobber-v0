node_modules/
dist/
.env
# Added by code-review-graph
.code-review-graph/
# Local Claude Code artifacts
.claude/worktrees/
SCOPE_LOCK.md
docs/superpowers/plans/
# Test artifacts
test-data/
uploads/
test-fixtures/
# SQLite runtime sidecar files
*.db-shm
*.db-wal
*.sqlite-shm
*.sqlite-wal
# Runtime SQLite database — the app bootstraps this from schema.sql on first run,
# so it is local runtime state, not seed data, and must not be tracked.
data/*.db
data/*.db-wal
data/*.db-shm
